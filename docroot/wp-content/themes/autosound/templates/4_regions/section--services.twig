{% extends '4_regions/section.twig' %}

{% set section = {
	class: 'section--services',
	contain: true
} %}

{% block sectionContent %}
	<div class="row">
		<div class="span-12">
			<h2 class="title">{{ title }}</h2>
		</div>
	</div>
	{% include '2_pieces/service-grid.twig' with {
		services: services
	} %}
	{% if cta %}
	<div class="section__cta centered">
		<a class="btn btn-white-outline" href="{{ cta.url }}">{{ cta.title }}</a>
	</div>
	{% endif %}
{% endblock %}
